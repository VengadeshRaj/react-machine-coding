{
  "name": "react-machine-coding",
  "private": true,
  "scripts": {
    "dev": "node scripts/run-app.js",
    "build": "pnpm -r run build"
  },
  "devDependencies": {
    "cross-env": "^10.1.0"
  },
  "dependencies": {
    "get-port": "^7.1.0"
  }
}
